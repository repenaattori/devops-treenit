name: Training
on: [workflow_dispatch]

permissions: 
  contents: write

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout git code
        uses: actions/checkout@v5
      - name: Build the app
        run: echo ${{secrets.DB_PW}}
           
